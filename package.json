{
  "name": "nightwatch-email-reporter",
  "version": "0.0.1",
  "description": "Email report generater from result json files from nightwatch",
  "bin": {
    "email-reporter": "bin/email-generate.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shjain",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.11.0",
    "glob": "^7.1.2",
    "swig": "^1.4.2"
  }
}
